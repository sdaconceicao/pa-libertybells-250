name: Pull Request Status Checks

on:
  pull_request:
    branches: [development, main]

permissions:
  contents: read

jobs:
  checks:
    name: Status Checks
    uses: ./.github/workflows/workflow-checks.yml
